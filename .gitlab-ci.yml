image: maven:3.8.4-jdk-11

.setup:
  artifacts:
    when: always
    paths:
      - "**/*.log"
    expire_in: 1 week

.docker_setup: &docker_setup
  - apt-get -q update && apt-get -q install -y docker.io
  - mkdir -p $HOME/.docker/cli-plugins/
  - wget --no-verbose -O $HOME/.docker/cli-plugins/docker-buildx $BUILDX_URL
  - chmod a+x $HOME/.docker/cli-plugins/docker-buildx
  - mkdir -p $TEST_MOUNT_DIRECTORY
  - docker buildx create --use --name chromia-cli --platform linux/amd64,linux/arm64,linux/arm/v8

.git_template: &git_setup |
  git remote set-url --push origin "https://oauth2:${BUILD_USER_GITLAB_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git"
  git config user.name "build-user core-dev"
  git config user.email build_user@chromaway.com


stages:
  - build
  - deploy

build:
  extends: .setup
  stage: build
  except:
    - master
    - tags
    - /^release\/.*$/
  script:
    - mvn $MAVEN_CLI_OPTS verify

deploy:
  before_script: *docker_setup
  extends: .setup
  stage: deploy
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - mvn $MAVEN_CLI_OPTS deploy -Drevision=$CI_COMMIT_TAG -Pgitlab-registry


variables:
  # This will suppress any download for dependencies and plugins or upload messages which would clutter the console log.
  # `showDateTime` will show the passed time in milliseconds. You need to specify `--batch-mode` to make this work.
  MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true"
  # As of Maven 3.3.0 instead of this you may define these options in `.mvn/maven.config` so the same config is used
  # when running from the command line.
  # `installAtEnd` and `deployAtEnd` are only effective with recent version of the corresponding plugins.
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version -DinstallAtEnd=true -DdeployAtEnd=true -s .gitlab-settings.xml"
  # Configure postgres service (https://hub.docker.com/_/postgres/)
  BUILDX_URL: https://github.com/docker/buildx/releases/download/v0.3.1/buildx-v0.3.1.linux-amd64
  TEST_MOUNT_DIRECTORY: /builds/$CI_PROJECT_PATH/mnt
  DOCKER_HOST: tcp://docker:2375
  POSTGRES_DB: postchain
  POSTGRES_USER: postchain
  POSTGRES_PASSWORD: postchain
  POSTCHAIN_DB_URL: jdbc:postgresql://postgres/postchain
  CHR_DB_URL: jdbc:postgresql://postgres/postchain

services:
  - postgres
  - name: docker:20-dind
    alias: docker
    # explicitly disable tls to avoid docker startup interruption
    command: [ "--tls=false" , "--experimental"]
# Cache downloaded dependencies and plugins between builds.
# To keep cache across branches add 'key: "$CI_JOB_NAME"'
cache:
  paths:
    - .m2/repository
